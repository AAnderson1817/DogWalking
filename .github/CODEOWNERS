# Code owners for Sanpo.
#
# CLAUDE.md singles out the money and trust paths for heightened rigour: a
# written safety argument in the PR, review aimed at the failure case, and a
# test that would have failed before the change. Merge authority itself sits
# with the session (the owner asked for minimal contact with the code), so
# these entries exist to surface the review request and to leave a durable
# record on the PR — not to gate the merge on a human.

# Default: everything is owned, so nothing lands unreviewed.
*                           @AAnderson1817

# --- Money and trust paths (CLAUDE.md "raise the bar") -----------------------

# Migrations are append-only and irreversible once applied to a real database.
/supabase/migrations/       @AAnderson1817

# Credit engine, ledger, Stripe, and the edge functions that move money.
/supabase/functions/        @AAnderson1817
/app/src/lib/credits.ts     @AAnderson1817

# The credential vault: door, lockbox and alarm codes for clients' homes.
/app/src/components/VaultFlows.tsx  @AAnderson1817
/app/src/screens/AccessVault.tsx    @AAnderson1817

# Deploy and CI. A change here can migrate or expose a live database.
/.github/workflows/         @AAnderson1817

# The seven non-negotiable invariants and the working agreement itself.
/CLAUDE.md                  @AAnderson1817
